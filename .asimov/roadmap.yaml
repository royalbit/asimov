# RoyalBit Asimov Roadmap
#
# WHAT to build - milestones only
# See: CHANGELOG.md for release history

current:
  version: "9.4.0"
  status: in_progress
  summary: "Unified Coding Standards for Programming Projects"
  goal: "Programming templates include doc + arch standards (not just code)"
  priority: HIGH
  deliverables:
    - "[x] Update Rust template: add documentation + architecture sections"
    - "[x] Update Go template: add documentation + architecture sections"
    - "[x] Update Flutter template: add documentation + architecture sections"
    - "[x] Update Python template: add documentation + architecture sections"
    - "[x] Update Node template: add documentation + architecture sections"
    - "[x] Use markdownlint-cli2 for doc linting (preferred)"
    - "[x] Tests: 343 lib + 59 main + 53 e2e = 456 tests passing"

next: []

previous:
  version: "9.3.0"
  status: released
  summary: "Coding Standards Protocol"
  goal: Quality

backlog:
  - "Session Controller: spawn() instead of exec() to supervise Claude Code, watch for compaction signals, auto-inject warmup before context loss - true self-healing"
  - "Deliverables Template (ADR-034): auto-append standard checklist to milestones based on project type - needs design for how to inject/display"
