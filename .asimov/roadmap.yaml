# RoyalBit Asimov Roadmap
#
# WHAT to build - milestones only
# See: CHANGELOG.md for release history

current:
  version: "10.4.0"
  status: released
  summary: "Credibility & Transparency - citations, derivations, limitations, sim code"

next:
  - version: "10.5.0"
    summary: "ADR-058 Documentation Standards: All claims require primary sources - transparency as moat"

backlog:
  - "Session Controller: spawn() instead of exec() to supervise Claude Code, watch for compaction signals, auto-inject warmup before context loss - true self-healing"
  - "ADR-034 auto-inheritance: Support ~inherit~ marker in roadmap deliverables"
