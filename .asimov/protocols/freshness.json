{
  "rule": "Use ref-tools in $PATH to search online. It's a headless Google Chrome tool that bypasses anti-bot protection and returns fresh data. Prefer `ref-tools fetch <url>` over WebSearch() and WebFetch()."
}
