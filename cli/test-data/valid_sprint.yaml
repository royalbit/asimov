# Valid sprint.yaml for testing
rules:
  max_hours: 4
  must_ship: true
  mantra: "Keep shipping"

phases:
  1_warmup:
    duration: "2-5 min"
    actions:
      - "Run asimov warmup"

anti_patterns:
  scope_creep: "Note it for NEXT session"

authority:
  principle: "Make decisions. Don't ask."
