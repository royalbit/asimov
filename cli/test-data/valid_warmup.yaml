# Valid warmup.yaml for testing
identity:
  project: "Test Project"
  tagline: "A test project"
  version: "1.0.0"

mission:
  problem: "Testing the validator"
  solution: "Run tests"

files:
  source:
    - "src/ - Source code"

session:
  start:
    - "Read warmup.yaml"
  during:
    - "Track progress"
  end:
    - "Run tests"

quality:
  tests: "All tests must pass"
